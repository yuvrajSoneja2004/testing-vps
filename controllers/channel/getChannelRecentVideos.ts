import { Request, Response } from "express";

export const getChannelRecentVideos = async (req: Request, res: Response) => {
  try {
    // TODO: Write the logic.
  } catch (error) {
    console.log(error);
  }
};
